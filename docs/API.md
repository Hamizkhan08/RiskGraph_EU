# API reference

> Generated by `scripts/render_api_docs.py` from the FastAPI OpenAPI schema. Interactive docs: `/docs` on a running server.

**RiskGraph EU API** v1.0.0 — Research prototype API (public/synthetic data). Not a detection system.

All payloads carry a `disclosure` field: synthetic data, research prototype, not a detection system.

| Method | Path | Parameters / body | Success | Errors |
|---|---|---|---|---|
| GET | `/health` | — | 200 | 422 |
| GET | `/api/datasets` | — | 200 | 422 |
| GET | `/api/dashboard` | `dataset` | 200 | 422 |
| GET | `/api/alerts` | `dataset`, `q`, `priority`, `status`, `sort`, `order`, `page`, `page_size` | 200 | 422 |
| GET | `/api/alerts/{case_id}` | `case_id`* | 200 | 422 |
| GET | `/api/cases` | `dataset`, `all`, `page`, `page_size` | 200 | 422 |
| GET | `/api/cases/{case_id}` | `case_id`* | 200 | 422 |
| GET | `/api/network/{case_id}` | `case_id`* | 200 | 422 |
| GET | `/api/explanations/{case_id}` | `case_id`* | 200 | 422 |
| POST | `/api/cases/{case_id}/decision` | `case_id`*, `x-api-key`, body `DecisionIn` | 201 | 422 |
| GET | `/api/model/metrics` | `dataset` | 200 | 422 |
| GET | `/api/model/calibration` | `dataset` | 200 | 422 |
| GET | `/api/monitoring` | `dataset` | 200 | 422 |
| GET | `/api/data-quality` | `dataset` | 200 | 422 |
| POST | `/api/predict` | `dataset`, body `PredictIn` | 200 | 422 |
| POST | `/api/simulate` | body `SimulateIn` | 200 | 422 |
| GET | `/api/audit` | `x-api-key` | 200 | 422 |

`*` = required. Path parameters are validated (`case_id` must match `^RG-(LI|HI)-\d{1,6}-\d{8}$`).

## Request models

### `DecisionIn`
* `decision`: string (required) — enum=['suspicious', 'false_positive', 'requires_review']
* `note`: string (optional) — maxLength=500
* `analyst`: string (optional) — maxLength=40, minLength=1, pattern=^[A-Za-z0-9 ._-]+$

### `DecisionOut`
* `case_id`: string (required)
* `decision`: string (required) — enum=['suspicious', 'false_positive', 'requires_review']
* `note`: string (required)
* `analyst`: string (required)
* `simulated`: boolean (required)
* `created_at`: string (required)
* `status`: string (required)

### `Health`
* `status`: string (required) — enum=['ok', 'degraded']
* `version`: string (required)
* `demo_mode`: boolean (required)
* `datasets`: array (required)
* `model_loaded`: boolean (required)
* `disclosure`: string (required)

### `Page`
* `items`: array (required)
* `total`: integer (required)
* `page`: integer (required)
* `page_size`: integer (required)
* `disclosure`: string (required)

### `PredictIn`
* `features`: object (required)

### `SimulateIn`
* `dataset`: string (optional) — enum=['LI', 'HI']
* `policy`: string (optional) — enum=['none', 'static', 'rolling', 'conformal']
* `alpha`: number (optional) — maximum=0.5, exclusiveMinimum=0.0
* `capacity_multiplier`: number (optional) — minimum=0.1, maximum=10.0
* `label_regime`: string (optional) — enum=['oracle', 'review_only']

## Behaviour notes

* **Auth:** none by default (public synthetic demo). If `RISKGRAPH_API_KEY` is set, `POST /api/cases/{id}/decision` and `GET /api/audit` require header `X-API-Key`.
* **Decisions** are labelled `simulated: true` while `RISKGRAPH_DEMO_MODE=true`.
* **Rate limit:** `RISKGRAPH_RATE_LIMIT_PER_MIN` per client address (default 120); `/health` is exempt; exceeded → `429` with `Retry-After`.
* **CORS:** exact origins from `RISKGRAPH_CORS_ORIGINS`; no credentials.
* **`/api/predict`** requires exactly the model's feature names (see `artifacts/demo/<DS>/model/feature_schema.json`); unknown/missing/non-finite → `422`.
* **`/api/simulate`** recomputes the decision engine on the stored test-window score stream for arbitrary policy, α ∈ (0, 0.5] and capacity multiplier ∈ [0.1, 10].
* Errors never include stack traces; unexpected failures return `500` with a `request_id`.
